#Author Nishant Pani 
# Purpose - Automated script to  run tests and collect results for the EE5351 project
# Script Spec - 1. Run the CPU and GPU tests 
# 2. Verify using diff that the CPU input is equal to CPU output, same for GPU , then the CPU and GPU outputs should match
# Use diff -qs
# 3. The script should extract the results and write a CSV file
# 4. The graphs that have to be plotted are?
# 5.  a. With different key sizes (128,192,256) corresponding to number of rounds (10,12,14)
# 6. Add the GPU and CPU information to the results.txt
